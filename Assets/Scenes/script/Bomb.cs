using System.Collections;
using System.Collections.Generic;
using System.Linq;
using UnityEngine;

public class Bomb : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        for() {
            target
        }
        List<int> masu_num = new List<int> {}
        */
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
